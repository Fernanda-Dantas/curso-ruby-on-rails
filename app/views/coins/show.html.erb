<p style="color: green"><%= notice %></p>

<%= render @coin %>

<div>
  <%= link_to t('links.edit'), edit_coin_path(@coin), class:"btn btn-primary mt-2" %>
  <%= link_to t('links.back'), coins_path, class:"btn btn-primary mt-2" %>

  <%= button_to t('links.delete'), @coin, method: :delete, data: {confirm: t('msg.confirm')}, class:"btn btn-danger mt-2" %>
</div>
